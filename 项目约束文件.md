# 蕾米埃尔桌宠 — 项目约束文件

> 所有已验证的跨平台构建/打包/运行约束。修改代码或 CI 前先看这里。
>
> 对应 CI 自动检查：`build.yml` 的 `checks` job（52 项，分十七大类：代码(#1-8) → 资源路径(#9-15) → CMake(#16) → CI 构建(#17-26) → 发布策略(#27-29) → Unicode 码表(#30) → 音频同步(#31-32) → 窗口缩放跟随(#33-37) → 资源即时应用(#38-40) → NAS 安全护栏(#41-44) → 滚轮缩放跨平台(#45) → 位置保存锚点(#46) → 动态资源与键位显示(#47) → 动态编译流程(#48) → 发布门禁(#49) → deskflow 级编译质量(#50) → deskflow 级平台矩阵(#51，Flatpak 暂不校验) → 代码风格(#52)）

---

## 一、代码约束

### 1.1 QSettings：必须设 orgName + appName
```cpp
app.setOrganizationName("RemiliaPet");
app.setApplicationName("RemiliaPet");
```
- 不设则存到 `Unknown Organization/Unknown Application`，各平台行为不确定
- ⚠️ 两者同名导致 `AppDataLocation` 在 Linux 上生成 `~/.local/share/RemiliaPet/RemiliaPet/` 双层路径，资源路径须用 `GenericDataLocation`（见 §1.12）

### 1.2 QSettings：退出保存用 `aboutToQuit`
```cpp
connect(qApp, &QApplication::aboutToQuit, this, &DesktopPet::saveSettings);
```
- ❌ `qApp->exit(0)` **不触发** `closeEvent`

### 1.3 QSettings：`loadSettings` 中禁止调 toggle 函数
- `toggleMouseTransparent()` / `toggleStayOnTop()` 第一行就是 `m_xxx = !m_xxx`，会把刚读出的值翻回去
- 必须抽取 `applyMouseTransparent()` / `applyStayOnTop()` 纯应用函数

### 1.4 QSettings：位置加载顺序 — 先 scale 后 position
- `applyScale()` 内部会 `move()`，若在 `loadSettings` 的 `move()` 之后调用会覆盖位置
- 正确顺序：加载 scale → applyScale() → 加载 position → move()
- 位置保存/恢复用**屏幕比例锚点**：保存 `window/rightRatio = (x+width-avail.left)/avail.width`、`window/yRatio`、`window/screenName`；恢复时按目标屏幕（按屏幕名查找，缺失回退主屏）当前可用区域换算——分辨率/屏幕大小变化后相对位置不变
- 运行中必须**实时跟随**：连接所有屏幕的 `QScreen::availableGeometryChanged`（含 `screenAdded` 新屏），300ms 防抖后按比例重算位置并 `updateSideWindowPositions()`
- 兼容旧设置：无 `rightRatio` 时用旧绝对右边缘 `window/right` 按当前主屏换算迁移
- 托盘「重置位置」= 移到右下角（ratio≈1.0），之后分辨率变化仍贴右下角

### 1.5 QSettings：位置校验用钳位，不用拒绝
```cpp
// ❌ 错误：靠近边缘直接丢弃
if (sx < avail.right() - 50 && sy < avail.bottom() - 50) move(sx, sy);
// ✅ 正确：钳位到有效范围
sx = qBound(0, sx, qMax(0, avail.right() - 50));
sy = qBound(0, sy, qMax(0, avail.bottom() - 50));
move(sx, sy);
```

### 1.6 QSettings：`saveSettings` 末尾加 `s.sync()`
- 确保立即写入磁盘，不依赖析构时延迟刷新

### 1.7 托盘提示
- 始终 `"蕾米埃尔桌宠"`，不要根据鼠标穿透状态追加括号内容

### 1.8 字体作用域
- `qApp->setFont()` 只影响主界面，不影响托盘菜单样式

### 1.9 音频方案：miniaudio + WAV（零外部依赖）
- 使用 miniaudio 单头文件库（v0.11.22+），直接调用系统音频 API（Windows WASAPI、macOS CoreAudio、Linux PulseAudio/ALSA）
- 已移除 Qt Multimedia 模块，无 GStreamer / FFmpeg / Qt Multimedia 依赖
- AudioPlayer 封装类（`src/audioplayer.h/cpp`）API 兼容 QSoundEffect
- 音频文件仍为 WAV 格式嵌入 QRC，miniaudio 原生支持 WAV 解码
- 多音效预加载多个 AudioPlayer 实例，共享一个全局 ma_engine

#### 1.9.1 QRC alias 与代码同步（⚠️ 易踩坑）
- 修改音频文件名/扩展名时，**必须同时更新两处**：
  1. resources.qrc 中的 <file alias="audio/xxx.xxx">
  2. petwindow.cpp 中 preloadSounds() 的 resolveResourcePath 调用
- 两处扩展名不一致时：代码请求 .wav 但 QRC alias 是 .mp3 → Qt 资源系统找不到 → 静默失败无声
- 禁止：代码用 .wav 但 QRC alias 留 .mp3（反之亦然）
- CI 自动检查：checks #31 校验 QRC alias 与 preloadSounds 中音频路径一致

#### 1.9.2 AudioPlayer 错误日志（⚠️ 跨平台调试必须）
- Windows GUI 应用（WIN32 子系统）无控制台，qDebug()/qWarning() 不可见
- audioplayer.cpp 必须包含文件日志（写入 %TEMP%/remilia_audio.log），记录：
  - ma_engine_init 成功/失败
  - setSource 路径有效性
  - ma_sound_init_from_file 结果
  - play 是否跳过（未加载状态）

#### 1.9.3 音频源解析防御性检查（⚠️ 无声排查第一关）
- `resolveSoundSource()`（preloadSounds / reloadSounds 共用）在 QRC 提取失败时必须输出 qWarning（含路径和排查提示），不能静默跳过
- 临时文件写入失败时必须输出错误
- 最终路径仍无效时必须输出警告
- 禁止：QRC 提取失败但无任何日志输出
- CI 自动检查：checks #32 校验 resolveSoundSource 内的防御性日志

### 1.10 窗口最小尺寸
- `setMinimumSize(100, 72)` 在构造函数中设置（最小可见尺寸）
- `setState()` 内 `setFixedSize()` 以 `m_nativeSizes[state] * m_scale` 为准
- **窗口与内容必须共用同一钳位尺寸**（`qMax(100/72, native * m_scale)`）：`setState()` / `applyScale()` 的窗口与 `m_currentTargetSize` 必须一致
- ❌ 禁止 `applyScale()` 用 `m_maxNativeSize` 定窗口：override GIF 尺寸差异会放大 max 与当前状态的差距，重置/启动后窗口按最大 GIF 放大而内容仍是当前状态尺寸，下一次滚轮缩放窗口会朝缩放反方向跳变
- 图片类侧窗（抽卡 DrawEffectWindow / 画画 DrawingEffectWindow）窗口尺寸必须按图片宽高比**等比缩放**：❌ 禁止对宽高分别用 `qMax(200)` / `qMax(150)` 独立钳位（小缩放时窗口会被压成方形，破坏初始形状）；✅ 只对宽度设 20 下限，高度始终 `sw * native.height() / native.width()`
- 侧窗必须跟随宠物：宠物窗口尺寸**任何**变化路径（滚轮缩放、重置、资源变更、`setState` 换状态）都要调用 `updateSideWindowPositions()` 让打开的侧窗重新缩放并贴靠；`setState` 内尺寸真正变化时必须同步调用（否则换状态后侧窗悬在原位）
- 抽卡窗口与桌宠窗口**等比联动**：创建时记录 `m_sizeRatio = 抽卡窗口宽 / 宠物窗口宽`，`updateScaleAndPosition` 按 `宠物窗口宽 × m_sizeRatio` 计算，❌ 禁止直接用 `draw.gif native × m_scale`（否则动画播放期间滚轮缩放时抽卡窗口跟滚轮不跟桌宠）
- 滚轮缩放跨平台约束：`wheelEvent` 必须兜底 `pixelDelta()`（macOS/Linux 触控板/惯性快速滚动时 `angleDelta` 为 0，只给像素增量，否则快速缩放突然失效，灵敏度换算 100px≈120°）；❌ 禁止每个滚轮事件直接 `saveSettings()`（QSettings INI 每事件 sync 在非 Windows 平台卡死 UI），必须用 500ms 防抖 `m_scaleSaveTimer` 落盘

### 1.11 macOS 专用
- 置顶/取消置顶 **禁止** 用 `setWindowFlags()`（会重建 NSWindow）
- 必须用原生 API：`objc_msgSend(window, "setLevel:", level)`
- 置顶级用 `CGWindowLevelForKey(kCGOverlayWindowLevelKey)` ≈102，不是硬编码 `5`

### 1.12 资源文件替换（目录扫描方式）
- 程序启动时扫描 `resources/` 目录（中文"资源"作为备选），子目录 `gif/` `audio/` `cards/` `drawing/`
- `loadOverrides()` 自动匹配文件名填充 `m_resourceOverrides`，`resolveResourcePath()` 优先查 override 再回退 QRC
- ResourceWindow 提供「替换」「全部替换」「打开」「打开全部目录」「删除」五个操作
- `applyResourceChanges()`（公有）为统一重载入口：重扫 override → 重算 GIF 原生尺寸 → 重载音效源 → 重建当前 GIF（宠物窗口自适应新尺寸）→ `updateSideWindowPositions()` 侧窗贴靠 → 刷新 ResourceWindow 状态行 + AuthorWindow 图片
- `scheduleResourceReload()`（公有）为防抖入口（300ms singleShot QTimer）：文件管理器 `filesChanged` 信号与 QFileSystemWatcher 事件均走此入口合并去重，到点统一调 `applyResourceChanges()`
- QFileSystemWatcher 监听资源根目录 + 4 个子目录 + 所有 override 文件（文件级监听覆盖 Linux 目录内原地改写不触发目录事件的问题），外部文件管理器改动同样即时生效；子目录被删/重建时 `syncResourceWatcher()` 自动重新挂载监听
- `refreshOverrides()` 仅清空并重新扫描 override 映射（GIF 重建逻辑已上移到 applyResourceChanges）
- 启动顺序约束：`loadOverrides()` 必须在 `preloadNativeSizes()` / `preloadSounds()` / `setState()` 之前执行，否则启动时 override 音频与初始 idle GIF 被忽略
- 后缀约束：GIF→`.gif`、音效→`.wav`、卡片/画作/作者图→`.png`
- 命名约束：编号资源按文件名匹配，无需正则校验
- ResourceWindow 类已拆分为独立文件 `src/resourcewindow.h/cpp`

### 1.13 资源目录跨平台路径
- **三级备选查找**：`QStandardPaths::GenericDataLocation + "/RemiliaPet/resources/"` → 程序根目录 `resources/` → 程序根目录 `资源/`
- `m_resourceDir` 初始值用 `QStandardPaths::writableLocation(QStandardPaths::GenericDataLocation) + "/RemiliaPet/resources/"`
- ❌ 禁止 `AppDataLocation`：orgName 和 appName 同名时 Linux 生成 `~/.local/share/RemiliaPet/RemiliaPet/` 双层路径
- Qt `AppDataLocation` 行为：Linux=`~/.local/share/<org>/<app>`、Windows=`AppData\Local\<org>\<app>`、macOS=`~/Library/Application Support/<org>/<app>`
- `GenericDataLocation` 绕过双层：Linux=`~/.local/share/`、Windows=`AppData\Local\`、macOS=`~/Library/Application Support/`，手动拼 `RemiliaPet/resources/`
- 所有路径统一用 `/`，Qt 内部自动转 Windows 反斜杠
- 目录结构固定：`resources/` 下含 `gif/` `audio/` `cards/` `drawing/` 四个子目录

### 1.14 打开目录按钮 — 内嵌文件浏览器（纯 Qt，零外部依赖）
- 打开目录按钮统一调用 openDirInFM()，内部创建 InAppFileDialog（QDialog 子类）
- InAppFileDialog 使用 QFileSystemModel + QTreeView，不依赖任何外部文件管理器
- 完整功能：向上/后退/前进/主页导航、路径跳转（点击路径标签输入）、双击打开/进入目录、表头排序（名称/大小/类型/日期）、底部状态栏（条目数/选中数）
- 新建：工具栏与右键菜单可新建文件/文件夹，创建后自动刷新并选中新条目
- 搜索过滤：顶部输入框按文件名实时过滤（QFileSystemModel::setNameFilters，目录始终可见）
- 拖拽导入：从外部拖入文件/目录自动复制到当前目录，同名提示覆盖确认，悬停高亮目标行
- 右键菜单：新建文件夹 / 新建文件 / 打开 / 删除 / 重命名 / 刷新
- 快捷键：F2 重命名、Delete 删除、F5 刷新、Enter 打开、Backspace/Alt+↑ 向上、Alt+←/→ 后退/前进、Esc 关闭
- 多选：Ctrl/Shift 多选，批量删除带总数确认
- 变更即时生效：导入/删除/重命名/新建后发 `filesChanged` 信号，经 `openDirInFM(dirPath, onFilesChanged)` 回调 → `scheduleResourceReload()` 防抖重载 GIF/音频/尺寸；外部文件管理器改动经 QFileSystemWatcher 同样即时生效，宠物换尺寸时所有打开窗口自动贴靠自适应
- 单实例：重复点击「打开」复用同一窗口（QPointer 追踪），关闭旧窗口
- 窗口风格与 ResourceWindow 一致（无边框、置顶、粉色圆角边框、深色面板）
- 尾部 / 在函数内部自动 chop(1) 清理
- 必须先 QDir::mkpath() 确保目标目录存在再打开
- 类文件：`src/inappfiledialog.h/cpp`

### 1.15 Unicode 字符码表

> **用途**：所有 `QString::fromUtf8()` 中使用 Unicode 转义的中文/符号字符串均在此登记，新增或修改字符串必须同步更新此表。

#### 1.15.1 右键菜单项

| Unicode 转义 | 实际显示 |
|-------------|----------|
| `\u2728 \u5E78\u8FD0\u62BD\u5361` | ✨ 幸运抽卡 |
| `\u23F0 \u95F9\u949F\u8BA1\u65F6` | ⏰ 闹钟计时 |
| `\U0001F3A8 \u968F\u673A\u753B\u753B` | 🎨 随机画画 |
| `\U0001F50A \u97F3\u91CF\u8C03\u8282` | 🔊 音量调节 |
| `\u2117 \u5236\u4F5C\u58F0\u660E` | ℗ 制作声明 |
| `\U0001F4C1 \u8D44\u6E90\u66FF\u6362` | 📁 资源替换 |
| `显示桌面宠物` | 显示桌面宠物 |
| `隐藏桌面宠物` | 隐藏桌面宠物 |
| `\u7F6E\u9876\u663E\u793A` | 置顶显示 |
| `\u9F20\u6807\u7A7F\u900F` | 鼠标穿透 |
| `\u2328 \u952E\u4F4D\u663E\u793A` | ⌨ 键位显示 |
| `\u2328 \u952E\u4F4D\u663E\u793A\u7F6E\u9876` | ⌨ 键位显示置顶 |
| `退出桌宠` | 退出桌宠 |

#### 1.15.2 计时器 UI

| Unicode 转义 | 实际显示 |
|-------------|----------|
| `\u8BBE\u5B9A\u5012\u8BA1\u65F6` | 设定倒计时 |
| `\u2715` | ✕ (关闭按钮) |
| `\u5206` | 分 |
| `\u79D2` | 秒 |
| `\u786E\u8BA4` | 确认 |
| `\u5012\u8BA1\u65F6\u4E2D` | 倒计时中 |
| `\u63D0\u9192` | 提醒 |
| `\u65F6\u95F4\u5230\u4E86\u54E6\uFF0C\u7EF3\u5320` | 时间到了哦，绳匠 |
| `\u65f6\u949f` | 时钟 |
| `\u62bd\u5361` | 抽卡 |
| `\u753b\u753b` | 画画 |
| `\u7ed3\u679c` | 结果 |
| `\u95f9\u949f` | 闹钟 |
| `\u6B63\u5E38` | 正常 |
| `\u97f3\u6548` | 音效 |
| `\u5361\u7247` | 卡片 |
| `\u753b\u4f5c` | 画作 |
| `\u5176\u4ed6` | 其他 |
| `\u542f\u52a8` | 启动 |
| `GIF\u52a8\u753b` | GIF动画 |
| `\u66ff\u6362` | 替换 |
| `\u91cd\u7f6e` | 重置 |
| `\u6253\u5f00` | 打开 |
| `\u5220\u9664` | 删除 |

#### 1.15.3 资源替换窗口

| Unicode 转义 | 实际显示 |
|-------------|----------|
| `\u8d44\u6e90\u66ff\u6362` | 资源替换 |
| `\u5168\u90e8\u66ff\u6362` | 全部替换 |
| `\u5df2\u66ff\u6362` | 已替换 |
| `\u9ed8\u8ba4` | 默认 |
| `\u5df2\u66ff\u6362 %1 \u4e2a\u6587\u4ef6` | 已替换 %1 个文件 |
| `\u6253\u5f00\u5168\u90e8\u76ee\u5f55` | 打开全部目录 |
| `\u6587\u4ef6\u6d4f\u89c8` | 文件浏览 |
| `\u9009\u62e9\u66ff\u6362\u6587\u4ef6` | 选择替换文件 |
| `\u9009\u62e9\u5305\u542b\u66ff\u6362\u6587\u4ef6\u7684\u76ee\u5f55` | 选择包含替换文件的目录 |
| `\u70b9\u201c\u66ff\u6362\u201d\u9009\u6587\u4ef6\uff0c\u6216\u201c\u5168\u90e8\u66ff\u6362\u201d\u6279\u91cf\u66ff\u6362` | 点"替换"选文件，或"全部替换"批量替换 |
| `PNG - card_%1` | PNG - card_%1 |
| `PNG - drawing_%1` | PNG - drawing_%1 |
| `/\u8D44\u6E90/` | /资源/ |

#### 1.15.4 字体设置窗口

| Unicode 转义 | 实际显示 |
|-------------|----------|
| `\u2501\u2501 \u5E38\u7528\u4E2D\u6587\u5B57\u4F53 \u2501\u2501` | ━━ 常用中文字体 ━━ |
| `\u2501\u2501 \u5176\u4ED6CJK\u5B57\u4F53 \u2501\u2501` | ━━ 其他CJK字体 ━━ |
| `\u2501\u2501 \u5176\u4ED6\u5B57\u4F53 \u2501\u2501` | ━━ 其他字体 ━━ |
| `\u5B57\u4F53\u5927\u5C0F` | 字体大小 |
| `\u5B57\u4F53\u7C97\u7EC6` | 字体粗细 |
| `\u7C97\u4F53` | 粗体 |
| `\u857E\u7C73\u57C3\u5C14\u684C\u5BA0` | 蕾米埃尔桌宠 |
| `蕾米埃尔桌宠` | 蕾米埃尔桌宠 |
| `重置大小 (100%)` | 重置大小 (100%) |
| `\u8BBE\u5B9A\u5012\u8BA1\u65F6` | 设定倒计时 |

#### 1.15.5 托盘菜单

| 字符串 | 实际显示 |
|--------|----------|
| `显示桌面宠物` | 显示桌面宠物 |
| `隐藏桌宠` | 隐藏桌宠 |
| `退出程序` | 退出程序 |
| `\u9F20\u6807\u7A7F\u900F` | 鼠标穿透 |
| `\u7F6E\u9876\u663E\u793A` | 置顶显示 |
| `调频结果` | 调频结果 |
| `重置位置` | 重置位置 |
| `\u2328 \u952E\u4F4D\u663E\u793A` | ⌨ 键位显示 |
| `\u2328 \u952E\u4F4D\u663E\u793A\u7F6E\u9876` | ⌨ 键位显示置顶 |
| `\u857E\u7C73\u57C3\u5C14\u684C\u5BA0` | 蕾米埃尔桌宠 |

#### 1.15.6 作者声明窗口

| 字符串截取 | 实际显示 |
|-----------|----------|
| `\u684C\u5BA0\u8BBE\u8BA1\u4F5C\u8005\uFF1Ab\u7AD9\u8BC9\u8BF4\u65B0\u8BED\uFF0C\u6B64\u684C\u5BA0\u4E3A\u514D\u8D39\u684C\u5BA0\n\u5982\u679C\u559C\u6B22\u8FD9\u4E2A\u684C\u5BA0\u7684\u8BDD\u8BB0\u5F97\u7ED9up\u70B9\u70B9\u5173\u6CE8\u54E6\n\u7531b\u7AD9\u661F\u5149-k\u4F7F\u7528AI\u5DE5\u5177\u79FB\u690D\u5E76\u4F18\u5316` | 桌宠设计作者：b站诉说新语，此桌宠为免费桌宠 / 如果喜欢这个桌宠的话记得给up点点关注哦 / 由b站星光-k使用AI工具移植并优化 |

#### 1.15.7 易错字符对照

| 正确 | Unicode | 易混字符 | Unicode | 出现位置 |
|------|---------|----------|---------|----------|
| 运 | `\u8FD0` | 返 | `\u8FD4` | 幸运抽卡 |
| 已 | `\u5DF2` | 己 | `\u5DF1` | 已替换 |
| 闹 | `\u95F9` | 闸 | `\u95F8` | 闹钟计时 |
| 设 | `\u8BBE` | 没 | `\u6CA1` | 设定倒计时 |
| 桌 | `\u684C` | 卓 | `\u5353` | 桌宠 |
| 宠 | `\u5BA0` | 宏 | `\u5B8F` | 桌宠 |
| 替 | `\u66FF` | 潜 | `\u6F5C` | 替换/已替换 |
| 画 | `\u753B` | 昼 | `\u663C` | 画画/画作 |
| 置 | `\u7F6E` | 罩 | `\u7F69` | 置顶显示 |
| 绳 | `\u7EF3` | 练 | `\u7EC3` | 时间到了哦，绳匠 |

#### 1.15.8 内嵌文件浏览器（InAppFileDialog）

| Unicode 转义 | 实际显示 |
|-------------|----------|
| `\u2190 \u5411\u4e0a` | ← 向上 |
| `\u21bb \u5237\u65b0` | ↻ 刷新 |
| `\u5237\u65b0` | 刷新 |
| `\u91cd\u547d\u540d` | 重命名 |
| `\u65b0\u6587\u4ef6\u540d:` | 新文件名: |
| `\u91cd\u547d\u540d\u5931\u8d25` | 重命名失败 |
| `\u76ee\u6807\u6587\u4ef6\u5df2\u5b58\u5728: %1` | 目标文件已存在: %1 |
| `\u65e0\u6cd5\u91cd\u547d\u540d: %1` | 无法重命名: %1 |

#### 1.15.9 键位显示

| Unicode 转义 | 实际显示 |
|-------------|----------|
| `\u7A7A\u683C` | 空格 |
| `\u56DE\u8F66` | 回车 |
| `\u9000\u683C` | 退格 |
| `\u5220\u9664` | 删除 |
| `\u2191` | ↑ |
| `\u2193` | ↓ |
| `\u2190` | ← |
| `\u2192` | → |
| `\u786e\u8ba4\u66ff\u6362` | 确认替换 |
| `\u76ee\u6807\u6587\u4ef6\u5df2\u5b58\u5728:\n%1\n\n\u662f\u5426\u66ff\u6362?` | 目标文件已存在: / 是否替换? |
| `\u5bfc\u5165\u6210\u529f` | 导入成功 |
| `\u5df2\u5bfc\u5165 %1 \u4e2a\u6587\u4ef6` | 已导入 %1 个文件 |
| `\u786e\u8ba4\u5220\u9664` | 确认删除 |
| `\u786e\u5b9a\u8981\u5220\u9664\n%1\n\u5417?` | 确定要删除 / 吗? |
| `\u5220\u9664\u5931\u8d25` | 删除失败 |
| `\u65e0\u6cd5\u5220\u9664: %1` | 无法删除: %1 |
| `\u2190 \u540e\u9000` | ← 后退 |
| `\u2192 \u524d\u8fdb` | → 前进 |
| `\u2302 \u4e3b\u9875` | ⌂ 主页 |
| `\u65b0\u5efa\u6587\u4ef6\u5939` | 新建文件夹 |
| `\u65b0\u5efa\u6587\u4ef6` | 新建文件 |
| `\u540d\u79f0:` | 名称: |
| `\u641c\u7d22` | 搜索 |
| `\u5171 %1 \u9879` | 共 %1 项 |
| `\u5171 %1 \u9879\uff0c\u5df2\u9009 %2 \u9879` | 共 %1 项，已选 %2 项 |
| `\u91ca\u653e\u4ee5\u5bfc\u5165\u5230 %1` | 释放以导入到 %1 |
| `\u65e0\u6548\u8def\u5f84` | 无效路径 |
| `\u8def\u5f84\u4e0d\u5b58\u5728\u6216\u4e0d\u662f\u76ee\u5f55: %1` | 路径不存在或不是目录: %1 |
| `\u521b\u5efa\u5931\u8d25` | 创建失败 |
| `\u65e0\u6cd5\u521b\u5efa: %1` | 无法创建: %1 |
| `\u65e0\u6cd5\u5220\u9664\u5f53\u524d\u76ee\u5f55: %1` | 无法删除当前目录: %1 |
| `\u65e0\u6cd5\u91cd\u547d\u540d\u5f53\u524d\u76ee\u5f55` | 无法重命名当前目录 |
| `\u786e\u5b9a\u8981\u5220\u9664\u4ee5\u4e0b %1 \u9879\u5417?\n\n%2` | 确定要删除以下 %1 项吗? / %2 |
| ` \u7b49 %1 \u9879` | 等 %1 项 |
| `\u65e0\u6cd5\u5220\u9664 %1 \u9879` | 无法删除 %1 项 |
| `\u5df2\u5220\u9664` | 已删除 |
| `\u5df2\u5220\u9664 %1 \u9879` | 已删除 %1 项 |

---

## 二、CMake 约束

### 2.1 macOS Bundle 图标
- `set(MACOSX_BUNDLE_ICON_FILE icon.icns)` **仅设置 plist 值**，不会自动复制文件
- 必须用 `add_custom_command(TARGET ... POST_BUILD)` 显式拷入 `Contents/Resources/`
- ❌ `list(APPEND SOURCES icon.icns)` 不可靠，跨 CMake 版本行为不一致

### 2.2 中文 target 名
- `set_target_properties(OUTPUT_NAME "蕾米埃尔桌宠_Qt6")` 可正常使用
- CI 脚本中引用文件名需用原始中文，不能转义

---

## 三、CI 构建约束

### 3.1 工作流分工
| 工作流 | 触发 | 职责 |
|--------|------|------|
| `build.yml` | push master / PR / tag `v*` | `checks` job 52 项约束检查 + `deepsec` job 漏洞审查（Shield）→ **门禁**：全部编译 job 依赖两者，检查/审查/格式化任一失败即终止编译；全平台编译矩阵 + 发布（仅 tag） |
| `build.yml` | push master / PR / tag `v*` | 动态 + 静态 job 全平台编译（**编译触发一致**：PR/push master 也编译，含 NAS 静态编译；仅 fork PR 被护栏拦截）+ **deskflow 级矩阵**：Windows ARM64、Linux 15 发行版容器矩阵（编译检查）、FreeBSD（Flatpak 清单保留在 deploy/linux/ 备用，暂未接入 CI）；**发布仅 tag**：`release`/`release-static`/`gitee` 均官方仓库+tag 双门禁（**主 CI**：动态版 `make_latest: true`，静态版 `make_latest: false`） |
| `dev.yml` | 手动触发 | Linux AppImage 开发测试（含 GStreamer） |
| `sync.yml` | push master / 手动 | 多仓同步：GitHub → 极狐 GitLab（含 tag 同步） |

> ⚠️ `static.yml` 已合并进 `build.yml`（`static`/`release-static` job），不再作为独立工作流存在

### 3.2 Linux .deb / .rpm 原生包
| 问题 | 约束 |
|------|------|
| **打包格式** | `dpkg-deb --build` 构建 .deb，`alien -r` 转 .rpm |
| **安装路径** | `/opt/remilia-pet/`，软链接 `/usr/bin/remilia-pet` |
| **桌面集成** | `.desktop` → `/usr/share/applications/`，图标 → `/usr/share/icons/` |
| **开机自启** | `/etc/xdg/autostart/remilia-pet.desktop` 随包安装 |
| **Wayland** | 子插件内嵌，launcher 默认 `QT_QPA_PLATFORM=xcb` 走 XWayland |
| **GStreamer** | 使用 miniaudio + WAV，不依赖 GStreamer / FFmpeg / Qt Multimedia |
| **Arch Linux** | 不打包 .pkg.tar.zst，用户下载便携版 .tar.gz |
| **键位显示钩子** | `keyhook_linux.cpp` 始终编译：`KEYHOOK_X11` 定义时才编译 X11 实现，否则空实现——保证 Flatpak SDK（可能缺 X11 头文件）等环境也能链接通过 |
| **便携版** | 同时生成 `tar.gz`，解压即用 |

### 3.3 GitHub Actions 版本约束
| 类别 | 约束 |
|------|------|
| **actions/checkout** | `@v5`（Node.js 24），`@v4` 已弃用 |
| **actions/upload-artifact** | `@v6` |
| **actions/download-artifact** | `@v7` |
| **Runner 标签** | `windows-2022`、`windows-11-arm`、`macos-26-intel` (Intel)、`macos-latest` (ARM64)、`ubuntu-latest`、`ubuntu-24.04-arm` |

### 3.4 macOS CI
- runner 预装 `aws/tap` 未签名 → `brew untap aws/tap 2>/dev/null || true` 消除警告
- `create-dmg --volicon icon.icns` 需要 `icon.icns` 在仓库根目录
- **Sequoia (15.x)+** 右键→打开已失效，用系统设置放行或 `xattr -d com.apple.quarantine`

### 3.5 Windows
- Inno Setup CI 自动安装路径：`$env:LOCALAPPDATA\InnoSetup\iscc.exe`（无空格，确保静默参数解析）
- 本地手动安装路径：`C:\Program Files (x86)\Inno Setup 6\iscc.exe`
- ⚠️ NAS 自托管 runner 的 PowerShell ExecutionPolicy 可能为 Restricted（步骤 .ps1 加载报 SecurityError/UnauthorizedAccess）：`build.yml` 中跑在 NAS 的 `static` 与 `gitee` 两个 job 均统一用 `-ExecutionPolicy Bypass` 执行 run 步骤；根治需在 NAS 管理员 PowerShell 执行 `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope LocalMachine`
- ⚠️ NAS runner 服务账号与工作区属主不一致（NETWORK SERVICE vs xg\xgxv）时 git 报 `dubious ownership`：run 步骤里先 `git config --global --add safe.directory "$env:GITHUB_WORKSPACE"`；根治：把 `_work` 目录属主改为服务账号，或将 runner 服务改为以属主账号运行
- ✅ **推荐：NAS runner 服务以普通用户账户运行**（不要用 NETWORK SERVICE / LocalSystem）：权限最小化、`_work` 属主自然一致；切换账号后若 runner 离线（DPAPI 凭据绑定原账户），需以新账户重新 `config.cmd` 注册
- ⚠️ Inno Setup 用户作用域静默安装**必须带 `/CURRENTUSER`**：否则安装器尝试写 HKLM 需要管理员，`/VERYSILENT` 下静默失败（iscc.exe 不存在）；安装后必须 `Test-Path` 校验并列出目录便于排查
- 🔒 NAS 自托管 runner 安全护栏（禁止项）：
  - `static` / `gitee` 两个 job 限定 `github.repository == 'kxgx/remilia_pet_qt'`，fork 仓库无法在本 runner 执行
  - job 内「安全护栏」步骤硬校验：❌ 禁止 fork PR 执行（同仓库 PR / push master / tag / dispatch 均允许——编译触发与动态一致）；发布仍由 release 系列 job 的 tag 门禁控制
  - 工作流级 `permissions: contents: read`（默认拒绝），各 job 按需最小授权（构建 job `actions: write`、发布 job `contents: write`、NAS job 不拿任何 write 权限）
  - ⚠️ 仓库设置还需开启：Settings → Actions → General → Fork pull request workflows **Require approval**（PR 修改 workflow 文件可绕过 job 内护栏，必须靠审批拦截）
  - ⚠️ NAS job 的 `run:` 脚本**内容必须全 ASCII**（Windows PowerShell 5.1 按 ANSI 解码无 BOM 的 .ps1，UTF-8 中文乱码后可能吞掉引号导致解析失败）；中文只允许出现在 step 名称与 `#` 注释里

---

## 四、版本发布约束

### 4.1 版本号同步
- **推送 tag 前必须同步版本号（三处）**：
  - `CMakeLists.txt` → `project(RemiliaPet VERSION x.y.z)`
  - `installer.iss` → `#define MyAppVersion "x.y.z"`
  - `build.yml` → `.deb` 打包中 `DEBIAN/control` 的 `Version: x.y.z`（两处：x64 + ARM64）
  - **tag 名必须与版本号一致**：`v1.0.9` ↔ 代码版本 `1.0.9`
- CI check #29 统一校验：CMakeLists.txt ↔ installer.iss ↔ build.yml .deb ↔ git tag，全部一致才通过

### 4.2 发布流程
```
# 1. 同步版本号（CMakeLists.txt + installer.iss + build.yml .deb 两处）
# 2. 提交推送
git add -A && git commit -m "..." && git push origin master
# 3. 等待 build.yml 的 checks/deepsec 门禁通过
# 4. 重新打 tag（旧 tag 已存在时）
git tag -d v1.0.x
git push origin :refs/tags/v1.0.x
git tag v1.0.x
git push origin v1.0.x
```
- ❌ 禁止 `git push --force` 覆盖远程 tag（某些平台禁止 force push protected tag）
- 本地 Windows 静态版/安装器手动编译后放 `release/`

### 4.3 发布策略
- `build.yml` 的 `release` job（动态版，主 CI）：`make_latest: true`，标记为 GitHub Latest Release
- `build.yml` 的 `release-static` job（静态版，副 CI）：`make_latest: false`，不标记 latest
- CI check #27/#28 自动校验，改错则拦截

---

## 五、Git 操作约束

### 5.1 排除编译产物
- `.gitignore` 含 `build/`、`build-static/`、`release/`、`Output/`

### 5.2 约束检查（build.yml 的 checks job）
- 每次 push master / PR / tag 自动运行 52 项检查（编译 job 的前置门禁，不通过即终止编译）
- 十七大类：代码约束(#1-8) → 资源路径(#9-15) → CMake(#16) → CI 构建(#17-26) → 发布策略(#27-29) → Unicode 码表(#30) → 音频同步(#31-32) → 窗口缩放跟随(#33-37) → 资源即时应用(#38-40) → NAS 安全护栏(#41-44) → 滚轮缩放跨平台(#45) → 位置保存锚点(#46) → 动态资源与键位显示(#47) → 动态编译流程(#48) → 发布门禁(#49) → deskflow 级编译质量(#50) → deskflow 级平台矩阵(#51，Flatpak 暂不校验) → 代码风格(#52)
- 任一失败阻止合入

### 5.3 CI Shell 脚本注意事项
- **`grep -oE` 而非 `grep -o`**：基础模式 `\+` 是字面量，`-E` 扩展模式下 `+` 才是「一个或多个」
- **`bash -e` 下 grep 需加 `|| true`**：`$(grep ...)` 在 errexit 下找不到匹配会直接退出脚本
- **echo 避免 `()`**：`echo ... (tag=$VAR)` 的括号会被 shell 解析为子 shell，改用 `[tag=$VAR]` 或双引号包裹
- **版本提取用两步法**：先 `grep '关键字'` 定位行，再 `grep -oE '数字正则'` 提取，避免括号/引号转义问题

### 5.4 代码风格（clang-format）
- 配置在仓库根 `.clang-format`：Allman 花括号 + 4 空格缩进 + `Type *ptr` + `ColumnLimit 0`（QSS 长字符串不可折行）+ `SortIncludes false`（保留平台 #ifdef 分组）
- CI check #52 用 clang-format **20.1.0**（PyPI）全量 `--dry-run --Werror` 校验（排除第三方 `miniaudio.h`）
- 本地格式化：`clang-format -i src/*.cpp src/*.h`（跳过 miniaudio.h）；VS2022 原生支持同一份配置
- ❌ 禁止改动代码后不跑格式化（CI 会拦截）

### 5.5 DeepSec 漏洞审查
- `build.yml` 的独立 `deepsec` job：**直接调用 DeepSec v0.2.0 发布 wheel 的 CLI**（`deepsec shield scan . --layer l1/l2 --format json`（**全仓库**，含 .github/workflows/CMakeLists/installer.iss/Python 脚本，不止 src/）），不用上游 composite action——`Unclecheng-li/DeepSec@main` 的 action 内部 setup-node 缓存路径解析有 bug（任何仓库都会在扫描前失败）
- 门禁：L1（幻觉包/硬编码密钥/不安全配置）+ L2（轻量 SAST）扫描，`critical` 发现即失败；结果数量写入 job 摘要；L3（LLM 语义层）未启用，无需 API key
- 安全约束：仅 GitHub 托管 runner 执行；job 权限 `contents: read`；❌ 禁止在任何自托管/NAS runner 上运行第三方审查
- ⚠️ 上游升级后若 action 修复可切回 action 形式；当前 CLI wheel 固定 v0.2.0

---

## 六、快速排查清单

| 症状 | 检查项 |
|------|--------|
| Linux .deb 安装失败 | dpkg-deb --build 和 Architecture 字段正确？ |
| Linux Arch 安装失败 | 使用便携版 .tar.gz 解压即用 |
| macOS 图标不显示 | CMakeLists.txt 有 POST_BUILD copy？ |
| macOS 窗口被覆盖 | level 用 `CGWindowLevelForKey` 而非硬编码 `5`？ |
| macOS Sequoia 打不开 | 系统设置放行或 `xattr -d` |
| 设置不保存 | `setOrganizationName` / `setApplicationName` 设了没？ |
| 退出后位置丢失 | `aboutToQuit` 连接了没？调的是 apply 不是 toggle？ |
| 窗口位置偏移 | 先 scale 后 position？校验是钳位还是拒绝？ |
| Linux 启动报 wayland | CI 装了 `qt6-wayland`？launcher 设了 `QT_QPA_PLATFORM=xcb`？ |
| Linux 音频 | miniaudio + WAV，无需 GStreamer / FFmpeg / Qt Multimedia |
| Linux 打开目录报错 | 使用内嵌 InAppFileDialog (QFileSystemModel + QTreeView)，无外部依赖 |
| 全平台无声音 | QRC alias 与 preloadSounds 路径扩展名一致？（check #31）; 看 %TEMP%/remilia_audio.log 定位 |
| 资源替换不生效 | `resources/` 目录存在？文件名/后缀匹配规则？ |
| 替换文件未加载 | `loadOverrides()` 扫描了对应子目录？`resolveResourcePath()` 包装了该路径？ |
| Inno Setup 找不到 | CI 路径 `$env:LOCALAPPDATA\InnoSetup\iscc.exe`？本地路径 `C:\Program Files (x86)\Inno Setup 6\iscc.exe`？ |
| CI 版本号检查失败 | CMakeLists.txt VERSION、installer.iss MyAppVersion、build.yml .deb Version 一致吗？ |
