# 蕾米埃尔桌宠 — 项目约束文件

> 为避免反复踩坑，汇总所有已验证的跨平台构建/打包/运行约束。修改代码或 CI 前先看这里。

---

## 一、CMake 约束

### 1.1 macOS Bundle 图标
- `set(MACOSX_BUNDLE_ICON_FILE icon.icns)` **仅设置 plist 值**，不会自动复制文件
- 必须用 `add_custom_command(TARGET ... POST_BUILD)` 显式拷入 `Contents/Resources/`
- ❌ `list(APPEND SOURCES icon.icns)` 不可靠，跨 CMake 版本行为不一致

### 1.2 `setWindowFlags` 在 macOS 上会重建 NSWindow
- macOS 置顶/取消置顶 **禁止** 用 `setWindowFlags()`
- 必须用原生 API：`objc_msgSend(window, "setLevel:", level)`
- 置顶级用 `CGWindowLevelForKey(kCGOverlayWindowLevelKey)` ≈102，不是硬编码 `5`

### 1.3 中文 target 名
- `set_target_properties(OUTPUT_NAME "蕾米埃尔桌宠_Qt6")` 可正常使用
- CI 脚本中引用文件名需用原始中文，不能转义

---

## 二、QSettings 持久化约束

### 2.1 必须设 orgName + appName
```cpp
app.setOrganizationName("RemiliaPet");
app.setApplicationName("RemiliaPet");
```
- 不设则存到 `Unknown Organization/Unknown Application`，各平台行为不确定

### 2.2 退出保存用 `aboutToQuit`
```cpp
connect(qApp, &QApplication::aboutToQuit, this, &DesktopPet::saveSettings);
```
- ❌ `qApp->exit(0)` **不触发** `closeEvent`

### 2.3 `loadSettings` 中禁止调 toggle 函数
- `toggleMouseTransparent()` / `toggleStayOnTop()` 第一行就是 `m_xxx = !m_xxx`，会把刚读出的值翻回去
- 必须抽取 `applyMouseTransparent()` / `applyStayOnTop()` 纯应用函数

### 2.4 位置加载顺序：先 scale 后 position
- `applyScale()` 内部会 `move()`，若在 `loadSettings` 的 `move()` 之后调用会覆盖位置
- 正确顺序：加载 scale → applyScale() → 加载 position → move()

### 2.5 位置校验用钳位，不用拒绝
```cpp
// ❌ 错误：靠近边缘直接丢弃
if (sx < avail.right() - 50 && sy < avail.bottom() - 50) move(sx, sy);
// ✅ 正确：钳位到有效范围
sx = qBound(0, sx, qMax(0, avail.right() - 50));
sy = qBound(0, sy, qMax(0, avail.bottom() - 50));
move(sx, sy);
```

### 2.6 `saveSettings` 末尾加 `s.sync()`
- 确保立即写入磁盘，不依赖析构时延迟刷新

---

## 三、CI 构建约束

### 3.1 Linux .deb / .rpm 原生包
| 问题 | 约束 |
|------|------|
| **打包格式** | 使用 `dpkg-deb --build` 构建 .deb，`alien -r` 转换为 .rpm |
| **安装路径** | 程序安装到 `/opt/remilia-pet/`，软链接 `/usr/bin/remilia-pet` |
| **桌面集成** | `.desktop` 文件安装到 `/usr/share/applications/`，图标到 `/usr/share/icons/` |
| **开机自启** | `/etc/xdg/autostart/remilia-pet.desktop` 随包安装，默认启用 |
| **Wayland** | Wayland 子插件（shell/graphics/decoration）随包内嵌，但 launcher 默认 `QT_QPA_PLATFORM=xcb` 走 XWayland 兼容层，最大兼容性 |
| **GStreamer 插件** | 使用 QSoundEffect + WAV，不依赖 GStreamer；deb 无需 GStreamer Depends |
| **Arch Linux** | 不再打包 .pkg.tar.zst。Arch/Manjaro 用户下载便携版 .tar.gz 解压即用（详见 README）。build.yml 不留 Arch job |
| **便携版** | 同时生成 `tar.gz`，解压即用，无需安装 |

### 3.1.1 GitHub Actions 版本约束
| 类别 | 约束 |
|------|------|
| **actions/checkout** | 使用 `@v5`（Node.js 24），`@v4` 已弃用 |
| **actions/upload-artifact** | 使用 `@v6`（Node.js 24 默认），`@v5` 仅初步支持 |
| **actions/download-artifact** | 使用 `@v7`（Node.js 24 默认），`@v6` 仅初步支持 |
| **Runner 标签** | `windows-latest` (Server 2025)、`macos-13` (Intel)、`macos-latest` (ARM64)、`ubuntu-latest`、`ubuntu-24.04-arm` |

### 3.2 macOS CI
- macOS runner 预装 `aws/tap` 未签名，`brew install` 前需 `brew untap aws/tap 2>/dev/null || true` 消除警告

### 3.3 macOS DMG
- `create-dmg --volicon icon.icns` 需要 `icon.icns` 在仓库根目录
- macdeployqt 可能覆盖 Info.plist，POST_BUILD 复制图标不受影响

### 3.4 macOS 运行
- **Sequoia (15.x)+** 右键→打开已失效
- 正确方法：系统设置 → 隐私与安全性 → "仍要打开"
- 或终端：`xattr -d com.apple.quarantine /path/to/app.app`

### 3.5 Windows
- Inno Setup 编译器路径：`$env:LOCALAPPDATA\Programs\Inno Setup 6\ISCC.exe`
- 不是 `C:\Program Files (x86)\Inno Setup 6\`

---

## 四、代码约束

### 4.1 托盘提示
- 始终 `"蕾米埃尔桌宠"`，不要根据鼠标穿透状态追加括号内容

### 4.2 字体作用域
- `qApp->setFont()` 只影响主界面，不影响托盘菜单样式

### 4.3 音频方案：QSoundEffect + WAV（无 GStreamer 依赖）
- QSoundEffect 走系统原生音频 API（Windows WASAPI、macOS CoreAudio、Linux PulseAudio/ALSA）
- 音频文件预转为 WAV 嵌入 QRC，无外部解码依赖
- 多音效预制多个 QSoundEffect 实例，互不干扰

### 4.4 窗口最小尺寸
- `setMinimumSize(140, 100)` 在构造函数中设置
- `setState()` 内 `setFixedSize()` 以 `m_nativeSizes[state] * m_scale` 为准

### 4.5 资源文件替换（目录扫描方式）
- 程序启动时扫描 `resources/` 目录（支持中文"资源"作为备选），子目录 `gif/` `audio/` `cards/` `drawing/`
- `loadOverrides()` 自动匹配文件名填充 `m_resourceOverrides`，`resolveResourcePath()` 优先查 override 再回退 QRC
- ResourceWindow 提供「打开目录」（QDesktopServices::openUrl）和「删除」按钮管理替换文件
- `refreshOverrides()` 在删除文件后清空并重新扫描
- 后缀约束：GIF->`.gif`、音效->`.wav`、卡片/画作/作者图->`.png`
- 命名约束：编号资源按文件名匹配，无需正则校验（用户自行保证命名正确）

### 4.6 资源目录跨平台路径约束
- `loadOverrides()` 三级备选查找：`QStandardPaths::GenericDataLocation + "/RemiliaPet/resources/"` → 程序根目录 `resources/` → 程序根目录 `资源/`，找到第一个存在的即用
- `m_resourceDir` 初始值用 `QStandardPaths::writableLocation(QStandardPaths::GenericDataLocation) + "/RemiliaPet/resources/"`，保证所有平台用户可写
- ❌ 禁止使用 `AppDataLocation`：当 `OrganizationName` 和 `ApplicationName` 同名（均为 "RemiliaPet"）时，Qt 在 Linux 上生成 `~/.local/share/RemiliaPet/RemiliaPet/` 双层路径，导致资源不可达
- Qt `AppDataLocation` 行为：Linux=`~/.local/share/<org>/<app>`、Windows=`AppData\Local\<org>\<app>`、macOS=`~/Library/Application Support/<org>/<app>`
- 用 `GenericDataLocation` 绕过 org/app 双层：Linux=`~/.local/share/`、Windows=`AppData\Local\`、macOS=`~/Library/Application Support/`，再手动拼接 `RemiliaPet/resources/`，跨平台统一可控
- 所有路径统一用 `/`，Qt 内部自动转 Windows 反斜杠

- 目录结构固定为：`resources/` 下含 `gif/` `audio/` `cards/` `drawing/` 四个子目录
- **打开目录按钮**（单行「打开」和顶部「打开全部目录」）必须先调用 `QDir::mkpath()` 确保目标目录存在，再调用 `QDesktopServices::openUrl()`
- **Linux 特化**：传给 `QDesktopServices::openUrl(QUrl::fromLocalFile(...))` 的路径**禁止**尾部 `/`，否则 `xdg-open` 报软件路径错误。使用 `if (dir.endsWith(QChar('/'))) dir.chop(1)` 清理
- 「打开全部目录」除根目录外，还需遍历创建四个子目录（与单行按钮行为一致）

---
## 五、Git 操作约束

### 5.1 排除编译产物
- `.gitignore` 不含 `build/`、`build-static/`、`release/`、`Output/`

### 5.2 版本发布
- 标签格式 `v*` 触发 CI 全平台构建 + Release
- **推送前必须同步版本号**：`CMakeLists.txt` 的 `project(... VERSION x.y.z)` 和 `installer.iss` 的 `#define MyAppVersion "x.y.z"` 必须一致
- 重新打 tag 流程（旧 tag 已存在时）：
  ```bash
  git tag -d v1.0.x
  git push origin :refs/tags/v1.0.x
  git tag v1.0.x
  git push origin v1.0.x
  ```
- ❌ 禁止 `git push --force` 直接覆盖远程 tag（某些平台禁止 force push protected tag）
- 发布策略：`build.yml`（主 CI，`make_latest: true`）负责标记 GitHub Latest Release；`static.yml`（副 CI，`make_latest: false`）不标记 latest
- 本地 Windows 静态版/安装器手动编译后放 `release/`

---

## 六、快速排查清单

| 症状 | 检查项 |
|------|--------|
| Linux .deb 安装失败 | dpkg-deb --build 和 Architecture 字段正确？ |
| Linux Arch 安装失败 | 使用便携版 .tar.gz 解压即用，无需安装 |
| macOS 图标不显示 | CMakeLists.txt 有 POST_BUILD copy？ |
| macOS 窗口被覆盖 | level 用的是 `CGWindowLevelForKey` 还是硬编码 `5`？ |
| 设置不保存 | `setOrganizationName` / `setApplicationName` 设了没？ |
| 退出后位置丢失 | `aboutToQuit` 连接了没？`loadSettings` 里调的是 apply 还是 toggle？ |
| 窗口位置偏移 | 先 scale 后 position 的加载顺序对吗？校验是钳位还是拒绝？ |
| Linux 启动报 wayland | CI 装了 `qt6-wayland` 吗？ |
| Linux 音频 | 使用 QSoundEffect 而非 QMediaPlayer，音频为 WAV 嵌入 QRC，无需 GStreamer |
| macOS Sequoia 打不开 | 右键->打开已废，用系统设置放行或 `xattr -d` |
| 资源替换不生效 | `resources/` 目录是否存在于程序目录？文件名/后缀是否匹配规则？ |
| 替换文件未加载 | `loadOverrides()` 扫描了对应子目录？`resolveResourcePath()` 包装了该路径？ |
| Inno Setup 找不到 | 路径是 `$env:LOCALAPPDATA\Programs\Inno Setup 6\` 吗？ |
| CI 发布版本号不一致 | CMakeLists.txt VERSION 和 installer.iss MyAppVersion 一致吗？ |
