# 蕾米埃尔桌宠 — 项目约束文件

> 为避免反复踩坑，汇总所有已验证的跨平台构建/打包/运行约束。修改代码或 CI 前先看这里。

---

## 一、CMake 约束

### 1.1 macOS Bundle 图标
- `set(MACOSX_BUNDLE_ICON_FILE icon.icns)` **仅设置 plist 值**，不会自动复制文件
- 必须用 `add_custom_command(TARGET ... POST_BUILD)` 显式拷入 `Contents/Resources/`
- ❌ `list(APPEND SOURCES icon.icns)` 不可靠，跨 CMake 版本行为不一致

### 1.2 `setWindowFlags` 在 macOS 上会重建 NSWindow
- macOS 置顶/取消置顶 **禁止** 用 `setWindowFlags()`
- 必须用原生 API：`objc_msgSend(window, "setLevel:", level)`
- 置顶级用 `CGWindowLevelForKey(kCGOverlayWindowLevelKey)` ≈102，不是硬编码 `5`

### 1.3 中文 target 名
- `set_target_properties(OUTPUT_NAME "蕾米埃尔桌宠_Qt6")` 可正常使用
- AppImage 打包脚本中引用文件名需用原始中文，不能转义

---

## 二、QSettings 持久化约束

### 2.1 必须设 orgName + appName
```cpp
app.setOrganizationName("RemiliaPet");
app.setApplicationName("RemiliaPet");
```
- 不设则存到 `Unknown Organization/Unknown Application`，各平台行为不确定

### 2.2 退出保存用 `aboutToQuit`
```cpp
connect(qApp, &QApplication::aboutToQuit, this, &DesktopPet::saveSettings);
```
- ❌ `qApp->exit(0)` **不触发** `closeEvent`

### 2.3 `loadSettings` 中禁止调 toggle 函数
- `toggleMouseTransparent()` / `toggleStayOnTop()` 第一行就是 `m_xxx = !m_xxx`，会把刚读出的值翻回去
- 必须抽取 `applyMouseTransparent()` / `applyStayOnTop()` 纯应用函数

### 2.4 位置加载顺序：先 scale 后 position
- `applyScale()` 内部会 `move()`，若在 `loadSettings` 的 `move()` 之后调用会覆盖位置
- 正确顺序：加载 scale → applyScale() → 加载 position → move()

### 2.5 位置校验用钳位，不用拒绝
```cpp
// ❌ 错误：靠近边缘直接丢弃
if (sx < avail.right() - 50 && sy < avail.bottom() - 50) move(sx, sy);
// ✅ 正确：钳位到有效范围
sx = qBound(0, sx, qMax(0, avail.right() - 50));
sy = qBound(0, sy, qMax(0, avail.bottom() - 50));
move(sx, sy);
```

### 2.6 `saveSettings` 末尾加 `s.sync()`
- 确保立即写入磁盘，不依赖析构时延迟刷新

---

## 三、CI 构建约束

### 3.1 Linux AppImage
| 问题 | 约束 |
|------|------|
| **FUSE2** | Ubuntu 24.04+ 默认只有 FUSE3，用户需 `apt install libfuse2` 或设 `APPIMAGE_EXTRACT_AND_RUN=1` |
| **Wayland 插件** | CI 依赖必须含 `qt6-wayland`，否则 Wayland 会话下报 `Could not find the Qt platform plugin "wayland"` |
| **GStreamer 警告** | AppImage 运行时会尝试加载系统 GStreamer 插件 → 版本冲突警告。必须用 `sed -i '2i export GST_PLUGIN_SYSTEM_PATH=' AppDir/AppRun` 注入环境变量 |
| **❌ binary wrapper** | 不要 mv + printf 创建 wrapper，路径会出错。改 AppRun 最可靠 |

### 3.2 macOS DMG
- `create-dmg --volicon icon.icns` 需要 `icon.icns` 在仓库根目录
- macdeployqt 可能覆盖 Info.plist，POST_BUILD 复制图标不受影响

### 3.3 macOS 运行
- **Sequoia (15.x)+** 右键→打开已失效
- 正确方法：系统设置 → 隐私与安全性 → "仍要打开"
- 或终端：`xattr -d com.apple.quarantine /path/to/app.app`

### 3.4 Windows
- Inno Setup 编译器路径：`$env:LOCALAPPDATA\Programs\Inno Setup 6\ISCC.exe`
- 不是 `C:\Program Files (x86)\Inno Setup 6\`

---

## 四、代码约束

### 4.1 托盘提示
- 始终 `"蕾米埃尔桌宠"`，不要根据鼠标穿透状态追加括号内容

### 4.2 字体作用域
- `qApp->setFont()` 只影响主界面，不影响托盘菜单样式

### 4.3 窗口最小尺寸
- `setMinimumSize(140, 100)` 在构造函数中设置
- `setState()` 内 `setFixedSize()` 以 `m_nativeSizes[state] * m_scale` 为准

---

## 五、Git 操作约束

### 5.1 排除编译产物
- `.gitignore` 不含 `build/`、`build-static/`、`release/`、`Output/`

### 5.2 版本发布
- 标签格式 `v*` 触发 CI 全平台构建 + Release
- 修改代码后 `git tag -f v1.0.x && git push origin v1.0.x --force`
- 本地 Windows 静态版/安装器手动编译后放 `release/`

---

## 六、快速排查清单

| 症状 | 检查项 |
|------|--------|
| macOS 图标不显示 | CMakeLists.txt 有 POST_BUILD copy？ |
| macOS 窗口被覆盖 | level 用的是 `CGWindowLevelForKey` 还是硬编码 `5`？ |
| 设置不保存 | `setOrganizationName` / `setApplicationName` 设了没？ |
| 退出后位置丢失 | `aboutToQuit` 连接了没？`loadSettings` 里调的是 apply 还是 toggle？ |
| 窗口位置偏移 | 先 scale 后 position 的加载顺序对吗？校验是钳位还是拒绝？ |
| Linux 启动报 wayland | CI 装了 `qt6-wayland` 吗？ |
| Linux GStreamer 满屏警告 | AppRun 里有没有 `export GST_PLUGIN_SYSTEM_PATH=`？ |
| Linux AppImage 打不开 | 用户装了 `libfuse2` 吗？ |
| macOS Sequoia 打不开 | 右键→打开已废，用系统设置放行或 `xattr -d` |
| Inno Setup 找不到 | 路径是 `$env:LOCALAPPDATA\Programs\Inno Setup 6\` 吗？ |
